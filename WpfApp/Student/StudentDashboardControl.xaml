<UserControl x:Class="WpfApp.StudentDashboardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BackgroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="tbHeader" FontSize="20" FontWeight="Bold" Margin="0,0,0,4"/>
        <TextBlock Grid.Row="1" x:Name="tbSubHeader" FontSize="13"
                   Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,16"/>

        <WrapPanel Grid.Row="2" x:Name="panelTiles"/>

        <TextBlock Grid.Row="4" Text="Upcoming Sessions" FontWeight="SemiBold" FontSize="14"/>
        <ListBox Grid.Row="5" x:Name="lstUpcoming" BorderThickness="0" Background="Transparent"/>
    </Grid>
</UserControl>
