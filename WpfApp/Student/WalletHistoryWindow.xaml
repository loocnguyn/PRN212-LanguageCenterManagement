<Window x:Class="WpfApp.WalletHistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ctl="clr-namespace:WpfApp.Controls"
        Title="Wallet Transaction History" Height="520" Width="820"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="tbBalance" Text="Current balance: 0"
                   FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
        <Button Grid.Row="1" Content="Reload" Width="90" Height="28"
                HorizontalAlignment="Left" Margin="0,0,0,8" Click="BtnReload_Click"/>

        <DataGrid Grid.Row="2" x:Name="dgTransactions"
                  AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding TransactionId}" Width="50"/>
                <DataGridTextColumn Header="Type" Binding="{Binding TransactionType}" Width="90"/>
                <DataGridTextColumn Header="Amount" Binding="{Binding Amount}" Width="105"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="90"/>
                <DataGridTextColumn Header="Created At" Binding="{Binding CreatedAt}" Width="140"/>
                <DataGridTextColumn Header="Provider Order" Binding="{Binding ProviderOrderId}" Width="190"/>
                <DataGridTextColumn Header="Note" Binding="{Binding Description}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <ctl:PagerBar Grid.Row="3" x:Name="pager" HorizontalAlignment="Left"
                      Margin="0,10,0,0" PageChanged="Pager_PageChanged"/>
    </Grid>
</Window>
