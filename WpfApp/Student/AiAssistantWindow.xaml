<Window x:Class="WpfApp.AiAssistantWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="AI Study Assistant" Height="680" Width="760"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">

    <DockPanel>
        <!-- Header -->
        <Border DockPanel.Dock="Top" Background="{StaticResource RoleStudentBrush}" Padding="20,14">
            <StackPanel Orientation="Horizontal">
                <ui:SymbolIcon Symbol="Bot24" Foreground="White" FontSize="26" Margin="0,0,12,0"/>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="AI Study Assistant" Foreground="White" FontSize="18" FontWeight="SemiBold"/>
                    <TextBlock x:Name="tbStudentName" Foreground="#EFE6FF" FontSize="12"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Margin="2,0,2,10" FontSize="12" TextWrapping="Wrap"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       Text="I can see your classes, timetable, attendance, grades, invoices and wallet — and only yours. Ask in Vietnamese or English."/>

            <!-- Conversation -->
            <Border Grid.Row="1" Style="{StaticResource Card}" Padding="4">
                <ScrollViewer x:Name="conversationScroll" VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="tbConversation" Margin="10" TextWrapping="Wrap" FontSize="13"
                               Foreground="{StaticResource TextPrimaryBrush}" LineHeight="21"/>
                </ScrollViewer>
            </Border>

            <!-- One-tap questions, so the first message is never a blank page -->
            <WrapPanel Grid.Row="2" x:Name="suggestions" Margin="0,10,0,0">
                <WrapPanel.Resources>
                    <Style TargetType="Button" BasedOn="{StaticResource PillButtonSubtle}">
                        <Setter Property="Margin" Value="0,0,8,8"/>
                        <Setter Property="Padding" Value="14,6"/>
                        <Setter Property="FontSize" Value="12"/>
                    </Style>
                </WrapPanel.Resources>
                <Button Content="Buổi học tới của mình là khi nào?" Click="SuggestionChip_Click"/>
                <Button Content="Mình còn nợ học phí bao nhiêu?"   Click="SuggestionChip_Click"/>
                <Button Content="Điểm của mình thế nào rồi?"        Click="SuggestionChip_Click"/>
                <Button Content="Mình vắng mấy buổi rồi?"           Click="SuggestionChip_Click"/>
            </WrapPanel>

            <!-- Input -->
            <Grid Grid.Row="3" Margin="0,4,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" x:Name="txtQuestion" Height="40" VerticalContentAlignment="Center"
                         Padding="10,0" FontSize="13" KeyDown="TxtQuestion_KeyDown"/>
                <Button Grid.Column="1" x:Name="btnSend" Content="Send" Style="{StaticResource PillButton}"
                        Width="90" Height="40" Margin="8,0,0,0" Click="BtnSend_Click"/>
            </Grid>
        </Grid>
    </DockPanel>
</Window>
