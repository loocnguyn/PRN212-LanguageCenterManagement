<Window x:Class="WpfApp.SessionRoomDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Change Session Room" Height="360" Width="460"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="110"/><ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" x:Name="tbTitle" Text="Change room for this session"
                   FontSize="16" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <TextBlock Grid.Row="1" Grid.ColumnSpan="2" x:Name="tbInfo" FontSize="12"
                   Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,0,0,16"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Room" VerticalAlignment="Center" Margin="0,0,0,10"/>
        <ComboBox  Grid.Row="2" Grid.Column="1" x:Name="cboRoom" Height="34" Margin="0,0,0,10"
                   DisplayMemberPath="Display"/>

        <TextBlock Grid.Row="3" Grid.Column="1" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"
                   TextWrapping="Wrap" Margin="0,0,0,12"
                   Text="Pick “Use class default room” to undo an override. The class's own room never changes."/>

        <TextBlock Grid.Row="4" Grid.Column="0" Text="Reason" VerticalAlignment="Top" Margin="0,4,0,10"/>
        <TextBox   Grid.Row="4" Grid.Column="1" x:Name="txtNote" Height="64" Margin="0,0,0,10"
                   TextWrapping="Wrap" AcceptsReturn="True" VerticalContentAlignment="Top" Padding="6,4"
                   VerticalScrollBarVisibility="Auto"/>

        <TextBlock Grid.Row="5" Grid.Column="1" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"
                   Text="e.g. “Room 101 under maintenance”." Margin="0,0,0,10"/>

        <StackPanel Grid.Row="7" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save"   Style="{StaticResource PillButton}"       Width="90" Margin="0,0,8,0" Click="BtnSave_Click"/>
            <Button Content="Cancel" Style="{StaticResource PillButtonSubtle}" Width="90" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
