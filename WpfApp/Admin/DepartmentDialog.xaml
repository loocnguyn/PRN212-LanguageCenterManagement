<Window x:Class="WpfApp.DepartmentDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Department" Height="290" Width="420"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/><ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" x:Name="tbTitle" Text="Add department"
                   FontSize="16" FontWeight="SemiBold" Margin="0,0,0,16"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Name" VerticalAlignment="Center" Margin="0,0,0,10"/>
        <TextBox   Grid.Row="1" Grid.Column="1" x:Name="txtName" Height="34"
                   VerticalContentAlignment="Center" Padding="6,0" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Access group" VerticalAlignment="Center" Margin="0,0,0,6"/>
        <ComboBox  Grid.Row="2" Grid.Column="1" x:Name="cmbAccess" Height="34" Margin="0,0,0,6">
            <ComboBoxItem Content="Academic Setup" Tag="ACADEMIC"/>
            <ComboBoxItem Content="Finance" Tag="FINANCE"/>
        </ComboBox>

        <TextBlock Grid.Row="3" Grid.Column="1" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"
                   TextWrapping="Wrap" Margin="0,0,0,10"
                   Text="Academic Setup unlocks students, classes &amp; enrollment. Finance unlocks invoices, payments, reports &amp; discounts."/>

        <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save"   Style="{StaticResource PillButton}"       Width="90" Margin="0,0,8,0" Click="BtnSave_Click"/>
            <Button Content="Cancel" Style="{StaticResource PillButtonSubtle}" Width="90" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
