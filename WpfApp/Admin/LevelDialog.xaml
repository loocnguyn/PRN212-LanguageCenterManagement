<Window x:Class="WpfApp.LevelDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Level" Height="220" Width="460"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Language" VerticalAlignment="Center" Margin="0,0,0,12"/>
        <TextBlock Grid.Row="0" Grid.Column="1" x:Name="tbLanguage" VerticalAlignment="Center"
                   FontWeight="SemiBold" Margin="0,0,0,12"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Level name" VerticalAlignment="Center" Margin="0,0,0,12"/>
        <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,0,0,12">
            <TextBox x:Name="txtName" Height="34"/>
            <TextBlock FontSize="11" Margin="0,4,0,0" Foreground="{StaticResource TextSecondaryBrush}"
                       TextWrapping="Wrap"
                       Text="e.g. A1 / B2 for CEFR, N5 for JLPT, HSK 3 for Chinese. Levels list in the order they are added, so add beginner levels first."/>
        </StackPanel>

        <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button Content="Save"   Style="{StaticResource PillButton}"       Width="90" Margin="0,0,8,0" Click="BtnSave_Click"/>
            <Button Content="Cancel" Style="{StaticResource PillButtonSubtle}" Width="90" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
