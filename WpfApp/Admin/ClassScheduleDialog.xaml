<Window x:Class="WpfApp.ClassScheduleDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Class Schedule" Height="320" Width="460"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="110"/><ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="Weekly schedule slot"
                   FontSize="16" FontWeight="SemiBold" Margin="0,0,0,16"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Class:" VerticalAlignment="Center" Margin="0,0,0,10"/>
        <ComboBox Grid.Row="1" Grid.Column="1" x:Name="cboClass" Margin="0,0,0,10"
                  VerticalContentAlignment="Center" DisplayMemberPath="Name" SelectedValuePath="ClassId"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Day of Week:" VerticalAlignment="Center" Margin="0,0,0,10"/>
        <ComboBox Grid.Row="2" Grid.Column="1" x:Name="cboDayOfWeek" Margin="0,0,0,10"
                  VerticalContentAlignment="Center" DisplayMemberPath="Key" SelectedValuePath="Value"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Slot:" VerticalAlignment="Center" Margin="0,0,0,10"/>
        <ComboBox Grid.Row="3" Grid.Column="1" x:Name="cboSlot" Margin="0,0,0,10"
                  VerticalContentAlignment="Center" DisplayMemberPath="Display" SelectedValuePath="SlotId"/>

        <TextBlock Grid.Row="4" Grid.Column="1" x:Name="tbHint" FontSize="11"
                   Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="6" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save"   Style="{StaticResource PillButton}"       Width="90" Margin="0,0,8,0" Click="BtnSave_Click"/>
            <Button Content="Cancel" Style="{StaticResource PillButtonSubtle}" Width="90" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
