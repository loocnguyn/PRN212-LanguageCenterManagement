<Window x:Class="WpfApp.TuitionDiscountDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Tuition Discount Detail" Height="520" Width="620"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="130"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Code:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="txtCode" Height="32" Margin="0,0,0,10"
                 Padding="8,0" VerticalContentAlignment="Center"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Name:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="txtName" Height="32" Margin="0,0,0,10"
                 Padding="8,0" VerticalContentAlignment="Center"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Type:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <ComboBox Grid.Row="2" Grid.Column="1" x:Name="cmbDiscountType" Height="32" Margin="0,0,0,10">
            <ComboBoxItem Content="PERCENT" IsSelected="True"/>
            <ComboBoxItem Content="FIXED"/>
        </ComboBox>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Value:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox Grid.Row="3" Grid.Column="1" x:Name="txtDiscountValue" Height="32" Margin="0,0,0,10"
                 Padding="8,0" VerticalContentAlignment="Center"/>

        <TextBlock Grid.Row="4" Grid.Column="0" Text="Condition:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <ComboBox Grid.Row="4" Grid.Column="1" x:Name="cmbConditionType" Height="32" Margin="0,0,0,10">
            <ComboBoxItem Content="NONE" IsSelected="True"/>
            <ComboBoxItem Content="EARLY_PAYMENT"/>
        </ComboBox>

        <TextBlock Grid.Row="5" Grid.Column="0" Text="Deadline days:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox Grid.Row="5" Grid.Column="1" x:Name="txtDeadlineDays" Height="32" Margin="0,0,0,10"
                 Padding="8,0" VerticalContentAlignment="Center"/>

        <Grid Grid.Row="6" Grid.ColumnSpan="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="130"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Start date:" VerticalAlignment="Center" Margin="0,0,10,10"/>
            <DatePicker Grid.Row="0" Grid.Column="1" x:Name="dpStartDate" Height="32" Margin="0,0,0,10"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="End date:" VerticalAlignment="Center" Margin="0,0,10,10"/>
            <DatePicker Grid.Row="1" Grid.Column="1" x:Name="dpEndDate" Height="32" Margin="0,0,0,10"/>

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Note:" VerticalAlignment="Top" Margin="0,0,10,0"/>
            <TextBox Grid.Row="2" Grid.Column="1" x:Name="txtNote" Height="80" Padding="8"
                     TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
        </Grid>

        <Grid Grid.Row="7" Grid.ColumnSpan="2" Margin="0,16,0,0">
            <CheckBox x:Name="chkActive" Content="Active" IsChecked="True" VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Save" Width="90" Height="34" Click="BtnSave_Click"/>
                <Button Content="Cancel" Width="90" Height="34" Margin="8,0,0,0" Click="BtnCancel_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
