<Window x:Class="WpfApp.TeacherManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:WpfApp"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Teacher Management" Height="640" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">

    <Window.Resources>
        <local:InitialsConverter x:Key="Initials"/>
        <DataTemplate x:Key="TeacherCellTemplate">
            <StackPanel Orientation="Horizontal">
                <Border Width="36" Height="36" CornerRadius="18"
                        Background="{StaticResource RoleTeacherBrush}" Margin="0,0,12,0">
                    <TextBlock Text="{Binding FullName, Converter={StaticResource Initials}}"
                               Foreground="White" FontWeight="SemiBold" FontSize="13"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="{Binding FullName}" FontWeight="SemiBold" FontSize="14"/>
                    <TextBlock Text="{Binding Username, StringFormat='@{0}'}"
                               FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                </StackPanel>
            </StackPanel>
        </DataTemplate>
    </Window.Resources>

    <DockPanel>
        <Border DockPanel.Dock="Top" Background="{StaticResource PrimaryBrush}" Padding="20,14">
            <StackPanel Orientation="Horizontal">
                <ui:SymbolIcon Symbol="Book24" Foreground="White" FontSize="26" Margin="0,0,12,0"/>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Teacher Management" Foreground="White" FontSize="18" FontWeight="SemiBold"/>
                    <TextBlock Text="Browse and manage teacher profiles"
                               Foreground="#DCEBFF" FontSize="12"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Style="{StaticResource Card}" Padding="12" Margin="0,0,0,14">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Search:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <TextBox x:Name="txtSearch" Width="240" Height="32" VerticalContentAlignment="Center" Padding="6,0"/>
                    <Button Content="Search" Style="{StaticResource PillButton}" Width="84" Margin="8,0,0,0" Click="BtnSearch_Click"/>
                    <Button Content="Reset" Style="{StaticResource PillButtonSubtle}" Width="84" Margin="8,0,0,0" Click="BtnReset_Click"/>
                </StackPanel>
            </Border>

            <Border Grid.Row="1" Style="{StaticResource Card}" Padding="0">
                <DataGrid x:Name="dgTeachers" AutoGenerateColumns="False" IsReadOnly="True"
                          SelectionMode="Single" RowHeight="52" BorderThickness="0"
                          Background="Transparent" MouseDoubleClick="DgTeachers_MouseDoubleClick">
                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="Teacher" Width="*" CellTemplate="{StaticResource TeacherCellTemplate}"/>
                        <DataGridTextColumn Header="Specialization" Binding="{Binding Specialization}" Width="140"/>
                        <DataGridTextColumn Header="Degree" Binding="{Binding Degree}" Width="110"/>
                        <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="120"/>
                        <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="190"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
                <Button Content="View"       Style="{StaticResource PillButtonInfo}"     Width="90"  Margin="0,0,8,0" Click="BtnView_Click"/>
                <Button Content="Add"        Style="{StaticResource PillButton}"        Width="90"  Margin="0,0,8,0" Click="BtnAdd_Click"/>
                <Button Content="Edit"       Style="{StaticResource PillButtonSuccess}" Width="90"  Margin="0,0,8,0" Click="BtnEdit_Click"/>
                <Button Content="Deactivate" Style="{StaticResource PillButtonDanger}"  Width="110" Click="BtnDelete_Click"/>
            </StackPanel>
        </Grid>
    </DockPanel>
</Window>
