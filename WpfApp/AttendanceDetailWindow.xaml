<Window x:Class="WpfApp.AttendanceDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Attendance Detail" Height="500" Width="700"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#F0F4F8" CornerRadius="6" Padding="12" Margin="0,0,0,8">
            <TextBlock x:Name="tbClassName" VerticalAlignment="Center" FontSize="14" FontWeight="Bold"/>
        </Border>

        <TextBlock Grid.Row="1" x:Name="tbSummary" FontSize="12" Foreground="#555" Margin="0,0,0,8"/>

        <DataGrid Grid.Row="2" x:Name="dgSessions"
                  AutoGenerateColumns="False" IsReadOnly="True" BorderThickness="1" BorderBrush="LightGray">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Session Date"  Binding="{Binding SessionDateDisplay}" Width="120"/>
                <DataGridTextColumn Header="Topic"         Binding="{Binding Topic}"               Width="180"/>
                <DataGridTextColumn Header="Status"        Binding="{Binding Status}"              Width="100"/>
                <DataGridTextColumn Header="Note"          Binding="{Binding Note}"                Width="*"/>
                <DataGridTextColumn Header="Recorded At"   Binding="{Binding RecordedAtDisplay}"   Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
