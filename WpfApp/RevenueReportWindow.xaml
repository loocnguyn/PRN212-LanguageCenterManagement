<Window x:Class="WpfApp.RevenueReportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Revenue Report" Height="660" Width="920"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- Date range filter -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="From:" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <DatePicker x:Name="dpFrom" Width="130" Height="30"/>
            <TextBlock Text="To:" VerticalAlignment="Center" Margin="12,0,6,0"/>
            <DatePicker x:Name="dpTo" Width="130" Height="30"/>
            <Button Content="Generate" Width="100" Height="30" Margin="10,0,0,0" Click="BtnGenerate_Click"/>
            <Button Content="Reset"    Width="80"  Height="30" Margin="6,0,0,0"  Click="BtnReset_Click"/>
        </StackPanel>
        <!-- Summary stats -->
        <GroupBox Grid.Row="1" Header="Summary" Padding="10" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Total Revenue:" Margin="0,0,6,0" FontWeight="Bold"/>
                <TextBlock x:Name="lblTotalRevenue" Foreground="Green" FontWeight="Bold" Margin="0,0,24,0"/>
                <TextBlock Text="Total Unpaid:" Margin="0,0,6,0" FontWeight="Bold"/>
                <TextBlock x:Name="lblTotalUnpaid" Foreground="Red"   FontWeight="Bold"/>
            </StackPanel>
        </GroupBox>
        <!-- Payment detail grid -->
        <DataGrid Grid.Row="2" x:Name="dgPayments"
                  AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Payment ID"  Binding="{Binding PaymentId}"   Width="90"/>
                <DataGridTextColumn Header="Invoice ID"  Binding="{Binding InvoiceId}"   Width="90"/>
                <DataGridTextColumn Header="Student ID"  Binding="{Binding StudentId}"   Width="90"/>
                <DataGridTextColumn Header="Amount"      Binding="{Binding Amount}"       Width="110"/>
                <DataGridTextColumn Header="Method"      Binding="{Binding Method}"       Width="120"/>
                <DataGridTextColumn Header="Date"        Binding="{Binding PaidAt}"       Width="140"/>
                <DataGridTextColumn Header="Note"        Binding="{Binding Note}"         Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
