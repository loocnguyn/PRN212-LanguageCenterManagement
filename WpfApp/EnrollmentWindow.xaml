<Window x:Class="WpfApp.EnrollmentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Enrollment" Height="620" Width="900"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <!-- Filters -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Student ID:" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <TextBox x:Name="txtStudentId" Width="120" Height="28" VerticalContentAlignment="Center" Padding="4,0"/>
            <TextBlock Text="Class ID:" VerticalAlignment="Center" Margin="12,0,6,0"/>
            <TextBox x:Name="txtClassId" Width="120" Height="28" VerticalContentAlignment="Center" Padding="4,0"/>
            <Button Content="Search" Width="80" Height="28" Margin="8,0,0,0" Click="BtnSearch_Click"/>
            <Button Content="Reset"  Width="80" Height="28" Margin="6,0,0,0" Click="BtnReset_Click"/>
        </StackPanel>
        <!-- DataGrid -->
        <DataGrid Grid.Row="2" x:Name="dgEnrollments"
                  AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single"
                  SelectionChanged="DgEnrollments_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID"          Binding="{Binding EnrollmentId}" Width="60"/>
                <DataGridTextColumn Header="Student ID"  Binding="{Binding StudentId}"    Width="100"/>
                <DataGridTextColumn Header="Class ID"    Binding="{Binding ClassId}"      Width="100"/>
                <DataGridTextColumn Header="Enrolled At" Binding="{Binding EnrolledAt}"   Width="160"/>
                <DataGridTextColumn Header="Status"      Binding="{Binding Status}"       Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
        <!-- Actions -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Enroll"     Width="90" Height="32" Margin="0,0,8,0" Click="BtnEnroll_Click"/>
            <Button Content="Drop"       Width="90" Height="32" Click="BtnDrop_Click"/>
        </StackPanel>
    </Grid>
</Window>
