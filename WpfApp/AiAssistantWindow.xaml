<Window x:Class="WpfApp.AiAssistantWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AI Study Assistant" Height="640" Width="720"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource PrimaryLightBrush}" CornerRadius="6" Padding="12" Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="🤖 AI Study Assistant" VerticalAlignment="Center" FontSize="15" FontWeight="Bold"
                           Foreground="{StaticResource PrimaryDarkBrush}"/>
                <TextBlock x:Name="tbStudentName" VerticalAlignment="Center" FontSize="13" Margin="12,0,0,0"
                           Foreground="{StaticResource PrimaryDarkBrush}"/>
            </StackPanel>
        </Border>

        <!-- Hint -->
        <TextBlock Grid.Row="1" x:Name="tbHint" Margin="2,0,2,8" FontSize="12" TextWrapping="Wrap"
                   Foreground="{StaticResource TextSecondaryBrush}"
                   Text="Ask about your own schedule, grades, or tuition — e.g. &quot;Mình còn nợ học phí bao nhiêu?&quot; or &quot;Điểm giữa kỳ của mình thế nào?&quot;"/>

        <!-- Conversation -->
        <Border Grid.Row="2" BorderBrush="{StaticResource BorderBrush2}" BorderThickness="1" CornerRadius="6"
                Background="{StaticResource SurfaceBrush}" Padding="4">
            <ScrollViewer x:Name="conversationScroll" VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="tbConversation" Margin="8" TextWrapping="Wrap" FontSize="13"
                           Foreground="{StaticResource TextPrimaryBrush}" LineHeight="20"/>
            </ScrollViewer>
        </Border>

        <!-- Input -->
        <Grid Grid.Row="3" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" x:Name="txtQuestion" Height="40" VerticalContentAlignment="Center"
                     Padding="8,0" FontSize="13" KeyDown="TxtQuestion_KeyDown"/>
            <Button Grid.Column="1" x:Name="btnSend" Content="Send" Width="90" Height="40" Margin="8,0,0,0"
                    Click="BtnSend_Click"/>
        </Grid>
    </Grid>
</Window>
